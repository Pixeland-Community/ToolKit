#include "stdafx.h"

// Change to take args?
int main()
{
    /* // Skipped for now
	// Load the app
	std::ifstream pFile("app.pixe");
	
	// Docompress the app

	*/ 

	// Temporarily look for an app folder.
	// Read every file and subdir 
	// Take every *.chai file
	// Load them
	
	


	
	// Read app properties

	// Create the framework objects needed by the app

	// Load any extra modules that the app asks for
	// Or auto load them and assume that there aren't unwanted modules

	// Eval the MAIN script file

	/* MAGIC HAPPENS */
	
	// Loop

	// Exit

	// Find out if things broke

	// If it broke record the known info
	// If the app properties are set to report the crash then do so

	// If things didn't break then we have to clean up

	// Destroy the framework objects

	// End the program

	return 0;
}

